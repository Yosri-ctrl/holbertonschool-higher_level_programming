#!/usr/bin/python3
class Square:
    """Square is a class containing only the
    the size attribut"""
    def __init__(self, size):
        """initialise the square class"""
        self.__size = size
